import './fonts.css'
import './global.scss'
// import "./firebase"

export const metadata = {
  title: 'Necxis',
  description: 'Necxis enable you to join and create fun activities by just capturing and uploading specific moments.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body >{children}</body>
    </html>
  );
}
